<div class="max-w-4xl mx-auto p-8">
  <div class="text-center mb-12">
    <div class="mb-4 text-6xl">🎨</div>
    <h1 class="text-6xl font-bold mb-3 bg-gradient-to-r from-red-600 via-purple-600 to-blue-600 bg-clip-text text-transparent">Chromatic</h1>
    <p class="text-2xl text-gray-700 mb-3 font-medium">Build color paths. Score exponentially.</p>
    <p class="text-lg text-gray-600 mb-8">Master 5 unique color rules to win!</p>

    <%= button_to "Start New Game", games_path, method: :post, class: "px-10 py-5 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-xl text-2xl font-bold hover:from-blue-700 hover:to-purple-700 shadow-lg hover:shadow-xl transition-all" %>
  </div>

  <% if @games.any? %>
    <div class="mt-12">
      <h2 class="text-2xl font-bold mb-4">Recent Games</h2>
      <div class="space-y-2">
        <% @games.each do |game| %>
          <div class="border rounded-lg p-4 hover:bg-gray-50">
            <%= link_to game_path(game), class: "flex justify-between items-center" do %>
              <div>
                <span class="font-semibold">Game <%= game.id %></span>
                <span class="text-gray-500 ml-4">Round <%= game.current_round %> of <%= game.total_rounds %></span>
              </div>
              <div class="text-right">
                <div class="text-sm text-gray-500">Score</div>
                <div class="font-bold">
                  You: <%= game.player_score %> | AI: <%= game.ai_score %>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

  <div class="mt-12 bg-gradient-to-br from-gray-50 to-blue-50 rounded-2xl p-8 border-2 border-gray-300 shadow-lg">
    <h3 class="text-2xl font-bold mb-6 text-gray-800">🎮 How to Play</h3>
    <div class="space-y-4 text-gray-800">
      <div class="flex items-start gap-3">
        <div class="text-2xl">🎯</div>
        <div>
          <strong class="text-lg">Goal:</strong> Build color paths following each color's rules
        </div>
      </div>
      <div class="flex items-start gap-3">
        <div class="text-2xl">💳</div>
        <div>
          <strong class="text-lg">Starting a path costs 2 cards</strong> (1 random card discarded)
        </div>
      </div>
      <div class="flex items-start gap-3">
        <div class="text-2xl">📈</div>
        <div>
          <strong class="text-lg">Scoring:</strong> (cards in path)² × combo multiplier
          <div class="text-sm text-gray-600 mt-1">1 card = 1pt | 2 cards = 4pts | 3 cards = 9pts | 4 cards = 16pts | 5 cards = 25pts</div>
        </div>
      </div>
      <div class="flex items-start gap-3">
        <div class="text-2xl">🌈</div>
        <div>
          <strong class="text-lg">Combo Multipliers:</strong> Play multiple colors for bonuses!
          <div class="text-sm text-gray-600 mt-1">2 colors = 1.2x | 3 colors = 1.4x | 4 colors = 1.7x | 5 colors = 2.0x RAINBOW!</div>
        </div>
      </div>
      <div class="flex items-start gap-3">
        <div class="text-2xl">🎨</div>
        <div>
          <strong class="text-lg">Color Rules:</strong>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-2 mt-3">
            <div class="flex items-center gap-2 bg-red-100 border-2 border-red-400 rounded-lg p-2">
              <span class="text-2xl">🔴</span>
              <div class="text-sm"><strong>Red:</strong> Jump by 2+ (max 8) e.g. 2→5→10</div>
            </div>
            <div class="flex items-center gap-2 bg-blue-100 border-2 border-blue-400 rounded-lg p-2">
              <span class="text-2xl">🔵</span>
              <div class="text-sm"><strong>Blue:</strong> Pairs + extend (max 10) e.g. 5,5,6→12,12,13</div>
            </div>
            <div class="flex items-center gap-2 bg-green-100 border-2 border-green-400 rounded-lg p-2">
              <span class="text-2xl">🟢</span>
              <div class="text-sm"><strong>Green:</strong> Consecutive (max 6) e.g. 4→5→6→7</div>
            </div>
            <div class="flex items-center gap-2 bg-yellow-100 border-2 border-yellow-400 rounded-lg p-2">
              <span class="text-2xl">🟡</span>
              <div class="text-sm"><strong>Yellow:</strong> +1 sequence (max 8) e.g. 3→4→5→6</div>
            </div>
            <div class="flex items-center gap-2 bg-purple-100 border-2 border-purple-400 rounded-lg p-2 md:col-span-2">
              <span class="text-2xl">🟣</span>
              <div class="text-sm"><strong>Purple:</strong> Any descending (no cap!) e.g. 20→15→9→3</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
